# Copy to the repo root on the VM as `.env` and fill in.
# NEVER commit the real .env (it is gitignored).
LASTFM_API_KEY=your_lastfm_api_key

# Optional overrides (defaults shown)
SERVER_PORT=3001
SERVER_HOST=0.0.0.0