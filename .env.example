# YouTube Data API v3 key (Google Cloud Console → APIs & Services → Credentials).
# Free tier: ~100 searches/day. Without it, search is disabled and
# paste-a-YouTube-URL still works.
YOUTUBE_API_KEY=